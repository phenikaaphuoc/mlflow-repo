name: Python Package using Conda

on: [push]

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
    - name: Add conda to system path
      run: |
        echo ${{secret.MLFLOW_TRACKING_URI}} >> $GITHUB_PATH
    

